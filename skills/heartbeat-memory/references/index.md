# Heartbeat — References

Progressive-disclosure links. Load this index via `skill_view("heartbeat-memory", "references/index.md")`; the linked files below live alongside the skill and are read directly from the skill directory.

| File | When to load |
|------|--------------|
| `../README.md` | Human-oriented setup, usage, and troubleshooting |
| `../scripts/beat.sh` | The tick — decides what to consider doing now |
| `../scripts/projects.sh` | Project registry feeding `project_work`/`own_projects` |
| `../scripts/set-circadian.sh` | Wake/sleep hour configuration |
