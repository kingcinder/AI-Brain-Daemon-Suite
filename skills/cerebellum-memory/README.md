# cerebellum-memory

Procedural precision and smoothness tracking. See `SKILL.md` for the full reference.

```bash
./install.sh --with-cron
./scripts/log-execution.sh --skill "writing-bash-scripts" --quality 0.8
./scripts/get-calibration.sh
```

Pairs naturally with `basal-ganglia-memory`.
