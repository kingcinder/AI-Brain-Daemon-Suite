# Cerebellum — References

Progressive-disclosure links. Load this index via `skill_view("cerebellum-memory", "references/index.md")`; the linked files below live alongside the skill and are read directly from the skill directory.

| File | When to load |
|------|--------------|
| `../README.md` | Human-oriented setup, usage, and troubleshooting |
| `../scripts/log-execution.sh` | Record one execution rep |
| `../scripts/get-calibration.sh` | Per-skill calibration overview |
